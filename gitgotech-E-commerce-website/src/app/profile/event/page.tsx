import Event from '@/components/CustomerDashboard/settings/Event';
import React from 'react';

const page = () => {
    return (
        <div>
              <Event />
        </div>
    );
};

export default page;