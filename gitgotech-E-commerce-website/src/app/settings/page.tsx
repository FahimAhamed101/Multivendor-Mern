import SettingsPage from '@/components/CustomerDashboard/settings/Settings';
import React from 'react';

const page = () => {
    return (
        <div>
               <SettingsPage />
        </div>
    );
};

export default page;