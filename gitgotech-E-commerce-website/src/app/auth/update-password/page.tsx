import CreatePasswordPage from '@/components/auth/UpdatePassword';
import React from 'react';

const page = () => {
    return (
        <div>
           <CreatePasswordPage />
        </div>
    );
};

export default page;